<h1>My Tasks</h1>
<div class="tasks">
  <% @tasks.each do |task| %>
  <div class="form-check task">
    <input class="form-check-input" type="checkbox" value="" id="defaultCheck1" <%= task.completed ? "checked" : "" %>>
      <p><%= link_to task.title, task_path(task) %>
      <p class="btn btn-light"><%= link_to "Delete",
              task_path(task),
              method: :delete,
              data: { confirm: "Are you sure?" } %></p>
    </label>
  </div>
  <% end %>
</div>

<p class="border-top new"><%= link_to "Add a new task", tasks_new_path %></p>

